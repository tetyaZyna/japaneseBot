-- liquibase formatted sql
-- changeset sasha:1
CREATE TABLE IF NOT EXISTS user_entity
(
    user_id           BIGINT NOT NULL,
    registration_date date,
    CONSTRAINT pk_userentity PRIMARY KEY (user_id)
);

--changeset sasha:2
CREATE TABLE IF NOT EXISTS hiragana_entity
(
    hieroglyph_id       BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    hieroglyph          VARCHAR(255),
    hieroglyph_pronouns VARCHAR(255),
    CONSTRAINT pk_hiraganaentity PRIMARY KEY (hieroglyph_id)
);

CREATE TABLE katakana_entity
(
    hieroglyph_id       BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
    hieroglyph          VARCHAR(255),
    hieroglyph_pronouns VARCHAR(255),
    CONSTRAINT pk_katakanaentity PRIMARY KEY (hieroglyph_id)
);